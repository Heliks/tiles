{
  "name": "tiles",
  "private": true,
  "version": "0.0.0",
  "publishConfig": {
    "registry": "http://193.31.26.163:4873/"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "testgame"
    ]
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.3.0",
    "@typescript-eslint/parser": "^4.3.0",
    "eslint": "^7.10.0",
    "eslint-plugin-import": "^2.22.1",
    "eslint-plugin-unicorn": "^22.0.0",
    "lerna": "^3.20.2",
    "rimraf": "^3.0.2"
  },
  "scripts": {
    "lint": "eslint 'packages/**/*.ts' --config .eslintrc.json",
    "link": "lerna link",
    "test": "lerna run test --no-prefix --stream",
    "build": "lerna run --stream --sort build",
    "build:clean": "rimraf packages/*/*/lib",
    "build:watch": "yarn build:clean && lerna run build:watch --no-prefix --parallel",
    "clean": "yarn build:clean && lerna clean && rm -rf node_modules"
  }
}
