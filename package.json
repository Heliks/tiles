{
  "name": "tiles",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "lint": "yarn workspaces run lint",
    "link": "lerna link",
    "test": "yarn workspaces run test --coverage --coverage-reporters=text",
    "build": "lerna run build",
    "build:watch": "lerna run build:watch"
  }
}
