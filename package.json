{
  "name": "tiles",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "lint": "lerna run lint --no-prefix --stream",
    "link": "lerna link",
    "test": "lerna run test --no-prefix --stream -- --coverage --coverage-reporters=text",
    "build": "lerna run build",
    "build:watch": "lerna run build:watch --no-prefix --parallel"
  }
}
