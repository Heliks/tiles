import { DisplayObject } from 'pixi.js';


/**
 * An object that can be drawn by the renderer.
 *
 * This is an alias type for the PIXI.JS `DisplayObject`.
 *
 * @see DisplayObject
 */
export type Drawable = DisplayObject;
