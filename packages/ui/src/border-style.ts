export class BorderStyle {

  /**
   * @param width Width (or thickness) in which border should be drawn.
   * @param color Color in which border should be drawn.
   */
  constructor(public width: number, public color: number) {}

}
